[package]
name = "cga-relay"
version = "1.30.118"
edition = "2021"

[dependencies]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
debug = false
split-debuginfo = "off"
incremental = false
overflow-checks = true
strip = "symbols"
