# Agent Session State / Brain Dump

**Objective:**
Write down exactly what you are currently doing, the last known successful step, and any immediate blockers.
This ensures the NEXT agent handling this repository knows exactly where you left off.

- **Current Task:** Check in, push, and publish the completed CGA `1.30.124` Admin, parser-runtime, and Relay reliability changes.
- **Last Action Taken:** Aligned Relay to `1.30.124`, passed the complete Python and Relay suites, built and installed the hardened local Relay candidate, and rebuilt the verified Docker Desktop bundle.
- **Failing Tests / Errors:** No failures remain. An initial system-Python run lacked the declared `tree-sitter-language-pack`, so the suite was rerun successfully in the repository `.venv`. The full parallel Relay suite exposed a test-only fallback-scope collision; using the libtest thread id when no thread name is available fixed it, and all 57 tests now pass. The local Relay candidate is intentionally unsigned; the tag-triggered workflow must produce the signed formal assets.
- **Next Steps:** Commit and push the scoped working tree, create tag `v1.30.124`, verify the signed GitHub release workflow, upload the verified Docker Desktop ZIP and checksum, then record final Relay aggregation.

## 2026-07-27 CGA-Relay Backend Outage Alert

- **Root Cause:** The tray process refreshed account login state but did not monitor `API_BASE_URL/health`, expose an outage icon state, or raise a system notification when the CGA Server Container was unavailable.
- **Fix:** Added an embedded yellow `R` resource, asynchronous startup and five-second health checks, 500 ms warning-icon alternation, recovery handling, and a `Shell_NotifyIconW` warning notification that appears once per continuous outage with explicit CGA Server Container startup guidance. The existing `Global\Nascousa.CGA-Relay.SingleInstance` mutex remains the process-wide startup gate.
- **Validation:** `cargo test` passed 9 unit and 48 integration tests. Focused tray status tests passed 4/4, and the two-process mutex regression passed. Editor diagnostics and Rust formatting passed. The isolated secure release build verified CFG, CET, ASLR/NX, no writable-executable sections, no symbol/debug path disclosure, the global mutex import/name, and absence of the debug test-instance scope.
- **Artifacts:** Local unsigned candidate `src/cga-relay/dist/cga-relay-1.30.120-windows-x64.zip` has SHA-256 `6b79935714b113c8e32bf95a0b9b7b06073d28d1bb2fc6f4e1406cdb9ff3b25e`; `cga-relay.exe` has SHA-256 `feda1afe653f7b7b644526456b53fe2cd9dba2f258cb8efbfea2fc756c64dd71`.
- **Release Replacement:** The secure release script now stages and verifies the candidate before stopping live processes, transactionally replaces each original executable path with rollback, and restores at most one prior tray command. Backup cleanup runs after availability is restored and retries transient Windows image locks for up to five seconds. The isolated replacement and explicit transient-lock regressions passed. The final real release restarted sole PID 55864 with `tray --config C:\Users\nasco\.cga\agent.env`; candidate, installed, and dist hashes match, and recursive residue inspection found zero `.cga-release-*` files.
- **Aggregation:** The restarted Relay exposes the current control schema at `127.0.0.1:17860` for project `J4BE9NUG2A`. Official `index-git-incremental` was retried through that existing instance and returned control HTTP 500 wrapping backend HTTP 403, so aggregation remains blocked and no direct fallback was counted as success.

## 2026-07-22 PR #41 Quality-Security Repair

- **Root Cause:** The merged README intentionally labels the 2026-06-02 benchmark as historical, while a stale test required the old heading. A fresh dependency audit also reported `PYSEC-2026-1325` in `ecdsa 0.19.2`, pulled only by `python-jose`; upstream has no planned fix.
- **Fix:** Updated the README contract test to require `Historical Benchmark Snapshot`, migrated HS256 issue/verify and unverified Microsoft account claim parsing to pinned `PyJWT[crypto]==2.13.0`, centralized the decode exception contract, removed `python-jose` from declared dependencies, and rejected configured JWT HMAC keys shorter than 32 bytes while generating a secure process-local fallback when no key is configured. Project, backend, Relay, lockfile, README, and health metadata now report `1.30.118`.
- **Validation:** The full Python coverage lane passed 353 tests at 72.81% coverage; live graph smoke and end-to-end lanes each passed; `pip-audit -r requirements.txt` reported no known vulnerabilities; Ruff passed all touched Python files; and `cargo test --locked` passed 8 unit plus 46 integration tests. The only remaining warning is the pre-existing Starlette `httpx` deprecation.
- **Security Evidence:** `PYSEC-2026-1325`/`CVE-2024-23342` is absent from the resolved requirements audit, no production source imports `jose`, invalid JWTs retain a single catchable error contract, and HS256 configuration now enforces the ADC 256-bit minimum.
- **Aggregation:** The local `cga-relay 1.30.117` selected the registered `ContextGraphAgent` account route but stopped before scanning with `CGA account login expired`; direct MCP/API fallback is not counted as completion.

## 2026-07-21 CGA 1.30.114 Commit Validation

- **Current Task:** Commit the Windows Azure CLI shim resolution, responsive Admin UI adjustments, explicit `Incremental Index` project action label, regression tests, and synchronized `1.30.114` metadata.
- **Last Action Taken:** Focused Python tests passed (`5 passed`), CGA-Relay tests passed (`32 passed`), Rust formatting passed, Python compileall passed, `git diff --check` passed, and the isolated release build succeeded. Shared-browser validation confirmed `v1.30.114`, `Incremental Index` actions, no 390 px horizontal overflow, responsive topbar hiding, accessible user-menu labeling, and a non-fixed footer.
- **Failing Tests / Errors:** The full Python suite reported two failures outside this diff: `test_readme_uses_live_multi_project_benchmark_results` expects a `Benchmark Snapshot` heading already absent in `HEAD`, and `test_cg_first_query_strategy_class_run` targets unchanged query-strategy code. The standard release executable remained locked by the active tray process, so release validation used `tmp/cga-relay-target-commit`. Official `cga-relay` incremental indexing and account sync were attempted and failed: project-token requests returned `403 Token is not valid for this project_id`, while the account-token path returned `401 Not authenticated`.
- **Next Steps:** Commit the validated source changes. Refresh relay authentication and retry `account/ContextGraphAgent` indexing and change aggregation separately; no direct API or `cga-mcp-server` fallback counts as official completion.

## 2026-07-10 CGA-Relay Branch Graph Support

- **Current Task:** Add backward-compatible temporary branch/ref graph routing, query fallback, post-merge promotion, and direct branch-aware Relay CLI commands.
- **Last Action Taken:** Added safe ref graph naming and aliases, branch-scoped indexing/query routing, graph-scoped cache keys, empty-branch fallback, `promote_ref`, true FalkorDB source graph deletion, Relay MCP exposure, plus `index git`, `index incremental`, and `refs promote` CLI wrappers that reuse the authenticated MCP bridge. Fixed portable release staging to include `extensions/` and removed a stale `$LASTEXITCODE` check that misreported a successful PowerShell child script as failed.
- **Failing Tests / Errors:** ContextGraph MCP pre-edit retrieval was blocked with HTTP 426 because the running SSE profile lacked valid CRYSTALS headers. The standard Relay release output remained locked by the active tray process, while isolated optimized builds succeeded. Official Relay dry-run selected `account/ContextGraphAgent` (`J4BE9NUG2A`) and found 364 scanned files, 39 changes, and no tombstones, but final submission still returned `HTTP request failed` because the configured project token/account login was unavailable; direct API fallback was not counted as completion. Final validation passed: Python `69 passed`, Rust `32 passed`, Ruff, compileall, formatting, diff, editor diagnostics, isolated release build, and Docker Desktop bundle build. `CGA-Docker-Desktop-1.30.111.zip` contains extensions, excludes generated dependency/build caches, and has SHA256 `5161A567BC4C6D5464503A0607DDF76A7A6BF2521B199B753C117B65A4EFCA40`.
- **Next Steps:** Refresh CGA-Relay account login or configure the project token, then retry official change aggregation. Refresh the standard `target/release/cga-relay.exe` after the active tray process exits if it remains locked. Future UI, merge automation, overlay queries, and TTL cleanup remain separate phases.

## 2026-06-19 Release Documentation Inclusion

- **Current Task:** Move the documentation alignment and relay `.nasco` exclusion cleanup into a new formal patch release so published source bundles include the updated docs.
- **Last Action Taken:** Bumped CGA app, README, AI-first docs, issue template, publishing example, and CGA-Relay version metadata to `1.30.99`.
- **Failing Tests / Errors:** Verification passed so far: `python -m compileall src/backend/main.py`, `python -m pytest src/tests/test_health.py -q --no-cov`, `cargo fmt`, `cargo test`, `cargo build --release`, editor diagnostics, `git diff --check`, and fixed-string stale-reference scans for public docs/version metadata.
- **Next Steps:** Completed: relay sync accepted the `1.30.99` version/doc updates, commits were pushed through `82b5535`, local desktop `/health` reports `1.30.99`, `CGA-Docker-Desktop-1.30.99.zip` was built with generated source outputs excluded, tag `v1.30.99` was pushed, the GitHub Release workflow completed successfully, and the zip plus `.sha256` asset were uploaded to `https://github.com/nascousa/cga/releases/tag/v1.30.99`. Docker Desktop zip SHA256: `8FC53DA707F0A351F6D471E69D69B9F4851868B209CCD6DFDDAF0BA7C05BD42B`.

## 2026-06-19 Documentation Release Alignment

- **Current Task:** Ensure documentation reflects the `1.30.98` AI-first default-collapse change and GitHub Release assets.
- **Last Action Taken:** Updated AI-first readiness/correlation docs to `1.30.98`, documented default-collapsed AI-first project cards, updated publishing guidance for Docker Desktop zip plus `.sha256` release assets, updated Docker Desktop bundle checksum guidance, and refreshed the bug report version placeholder.
- **Failing Tests / Errors:** Verification passed: stale-version/doc checks found no outdated public user-facing release references; editor diagnostics and `git diff --check` passed; `cargo fmt`, `cargo test`, and `cargo build --release` passed for CGA-Relay. Relay dry-run confirmed `.nasco` no longer appears in `changed_paths` and will submit 4 tombstones for previously synced ignored local notes.
- **Next Steps:** Completed relay cleanup sync: CGA accepted 4 snapshots and 4 tombstones for ignored `.nasco` local notes. Commit and push the documentation alignment plus relay `.nasco` exclude/tombstone fix.

## 2026-06-19 AI-First Default Collapsed Projects

- **Current Task:** Make `/admin/ai-first` render all project readiness cards collapsed by default.
- **Last Action Taken:** Switched the Admin UI preference model from stored collapsed project ids to stored expanded project ids, so fresh project cards default to collapsed while explicit user expansions persist.
- **Failing Tests / Errors:** Verification passed: `python -m compileall src/backend/main.py`, `python -m pytest src/tests/test_health.py -q --no-cov`, Admin inline script syntax check, `git diff --check`, editor diagnostics, local `/health` (`1.30.98`), and shared browser DOM snapshot showing all AI-first project cards collapsed by default. Browser credential re-entry was intentionally skipped to avoid exposing local secrets after the session expired during reload.
- **Next Steps:** Completed: source changes were relay-synced and pushed as `9c82c43`, local desktop health reports `1.30.98`, the Docker Desktop release zip was created at `deploy/docker-desktop/dist/releases/CGA-Docker-Desktop-1.30.98.zip` with SHA256 `C0787E6B4A87F653BC19C95EDED6AD82A45A4DF5798615FD28B9BEBDD57AB144`, tag `v1.30.98` was pushed, the GitHub Release workflow completed successfully, and the zip plus `.sha256` asset were uploaded to `https://github.com/nascousa/cga/releases/tag/v1.30.98`.

## 2026-06-18 CGA-Relay Secret-Safe Scan Excludes

- **Current Task:** Make CGA-Relay skip generated and secret-like paths before recursive scans so sync stays small and safe.
- **Last Action Taken:** Updated relay version metadata to `1.30.96`, added built-in excludes for dependency/build/cache directories and secret-like files, updated relay config example, rebuilt `src/cga-relay/target/release/cga-relay.exe`, and successfully submitted `account/ContextGraphAgent` via relay sync (`accepted: true`, `snapshot_count: 353`).
- **Failing Tests / Errors:** Verification passed: `cargo fmt --check`, `cargo test`, `cargo build --release`, `python -m compileall src/backend/main.py`, `python -m pytest src/tests/test_health.py -q --no-cov`, and `git diff --check`.
- **Next Steps:** Commit and push the `1.30.96` relay scan-exclude fix.

## 2026-06-18 Upgrade Center, AI-First Evidence, And FalkorDB Persistence

- **Current Task:** Add Admin UI upgrade readiness, CI/PR signal imports, PR evidence links, policy-derived AI-first gates, and persistent FalkorDB volume mounts.
- **Last Action Taken:** Added `/api/admin/upgrade/status`, Upgrade Center UI, GitHub and Azure DevOps import into `ai_first_signals`, signal matching inside `build_evidence_pack`, Admin UI controls for imports, saved evidence PR-template export, `signal_evidence` Markdown output, readiness/evidence `policy_gates`, Admin policy gate rendering, FalkorDB compose volume fixes, and bumped README/backend/docs metadata to `1.30.95`.
- **Failing Tests / Errors:** Final focused verification passed after the `1.30.95` increment: `pytest src/tests/test_ai_first_api.py src/tests/test_mcp_tools.py src/tests/test_cga_relay_router.py -q` (67 passed), `python -m compileall src/backend/ai_first src/backend/main.py src/tests/test_ai_first_api.py`, Admin inline script syntax check, and `git diff --check`. This CPMD pass also passed `python -m pytest src/tests/test_ai_first_api.py src/tests/test_health.py src/tests/test_template_quality.py -q --no-cov`, `python -m compileall src/backend/ai_first src/backend/main.py`, Admin inline script syntax check, editor diagnostics, and `git diff --check`. Local live smoke passed for `/health` (`1.30.95`), `/admin/ai-first` ADO/PR UI, and ADO endpoint registration. Local `cga-relay sync` remains blocked by expired account auth: `/api/auth/cga-relay/sync` returns `401 Unauthorized`, and `CGA_API_KEY` / `CGA_DEVELOPER_TOKEN` are not configured. Relay dry-run is healthy after local excludes were narrowed (`356` scanned files, `84520` excluded).
- **Next Steps:** Code was committed and pushed as `f88ce64` on `dev/readme-docs-restructure-20260603`. Re-run `cga-relay sync --config %USERPROFILE%\.cga\relay.env --namespace account --project-tag ContextGraphAgent` after refreshing local relay login or setting `CGA_DEVELOPER_TOKEN`.

## 2026-06-18 AI-First Project Collapse State

- **Current Task:** Make each project card on `/admin/ai-first` collapsible and remember the last collapsed state.
- **Last Action Taken:** Added per-project collapse state in `localStorage`, collapse controls, and collapsed-content styling in `src/frontend/index.html`; aligned README metadata with app version `1.30.88`.
- **Failing Tests / Errors:** No errors found in editor diagnostics; inline script syntax check passed; browser DOM validation confirmed per-project collapse and reload persistence. An initial direct CGA MCP indexing attempt used the wrong path and was corrected.
- **Next Steps:** Ready for review; rebuild was applied to the local desktop service at `http://localhost:18001`. CGA-Relay stdio MCP successfully queued incremental indexing for `/repos/ContextGraphAdmin` with job `bac84286-0fc8-4794-b2bf-eb517559da15` (`changed_count: 303`). That job is still pending because the earlier accidental `/repos` full-index job `5eb08404-c4f5-4f74-ab35-a84354f3e88b` is processing ahead of it.

## 2026-06-15 ADC Standards Refresh

- **Current Task:** Align ContextGraphAdmin ADC files with the latest local ADC template standards.
- **Last Action Taken:** Synced `.adc/standards`, added missing PR checklist, normalized rule pointer files, merged latest prompt-rule guidance while preserving local Docker and `cga-relay` paths, added a relay-first ContextGraph MCP profile, and bumped README/backend metadata to `1.30.84`.
- **Failing Tests / Errors:** No executable product tests were required for the documentation/configuration-only standards refresh.
- **Next Steps:** Review the ADC diff, then commit through the normal `dev/*` branch workflow if publishing this standards refresh.

## 2026-07-27 Parser Language Controls

- **Current Task:** Add Settings > Indexing controls that let users enable or disable every supported parser language and enforce those choices during indexing.
- **Last Action Taken:** Added a canonical 51-language catalog, persisted `disabled_parser_languages`, filtered full and incremental indexing (including conventional filenames and shared `.m` routing), removed stale graph data for newly disabled files, added searchable grouped controls with bulk actions, and aligned the desktop parser runtime packages.
- **Validation:** The complete related suite passed 91/91 in `.venv`; the packaging contract, strict touched-file lint, editor diagnostics, and `git diff --check` passed. The rebuilt desktop service is healthy, reports 51 runtime languages with none disabled, and loads the bundled C# grammar. Browser checks confirmed 7/34/10 groups, Rust search, 51-to-50 single-toggle state, 0/51 and 51/51 bulk actions, and non-overlapping responsive desktop/mobile layouts. The browser used unsaved in-memory data and did not change persisted settings.
- **Failing Tests / Errors:** No feature failures remain. Existing admin topbar content, not the language panel, causes horizontal overflow at an intermediate integrated-browser width. The full repository suite was not run.
- **Next Steps:** Review and commit the scoped parser-language settings changes; no additional implementation work is pending.

## 2026-07-27 Admin Tab Refresh Persistence

- **Current Task:** Keep the active Admin tab selected after a browser refresh instead of returning to the landing content.
- **Root Cause:** Top-level Admin tabs already had URL routes, but the nine Settings subtabs shared `/admin/settings`; refreshing therefore discarded the active Settings subtab and restored General.
- **Fix:** Added canonical Settings subtab deep links, History API updates on subtab selection, URL restoration during initialization and `popstate`, invalid-subtab fallback to General, and FastAPI deep-link coverage. General remains backward compatible at `/admin/settings`; the other subtabs use `/admin/settings/<subtab>`.
- **Validation:** The regression test failed before the implementation and passed afterward. The focused health/Admin/viewer suite passed 18/18, inline JavaScript syntax validation passed, and editor diagnostics reported no errors. The rebuilt `cga-desktop-api` is healthy at `127.0.0.1:18001`, reports `1.30.121`, and all nine Settings routes return HTTP 200 with the deployed restoration logic.
- **Browser / Retrieval Notes:** The shared browser confirmed the pre-fix `Settings > Indexing` state at `/admin/settings`; refreshing exposed the missing subtab route but also expired the local login session, so no credential was requested or fabricated. The pre-edit ContextGraph retrieval returned no matching context, and the bounded local fallback identified the owning inline routing functions and neighboring tests.
- **Aggregation:** The sole running Relay process (local control API `127.0.0.1:17860`) accepted official incremental indexing for project `J4BE9NUG2A`. Job `b6665f30-84ae-4cdb-b7a3-5c67cd6148a7` completed with 19 files processed, 4,742 symbols, and zero errors.
- **Next Steps:** No implementation work remains; the shared browser requires the user to sign in again before an authenticated visual refresh check.

## 2026-07-28 Mojo Extension Text Label

- **Current Task:** Replace the fire emoji shown beside Mojo in Settings > Indexing with an explicit text label while preserving recognition of real `.🔥` source files.
- **Root Cause:** The parser language catalog correctly exposed `.🔥` as a Mojo extension, but the Settings renderer joined raw extension values directly into the visible label.
- **Fix:** Added a UI-only file-label map that renders `.🔥` as `fire emoji extension`; indexing, filtering, runtime configuration, and the canonical Mojo extension registry remain unchanged. Updated README and backend metadata to `1.30.122` with the `2026-07-28` release date.
- **Validation:** The new regression test failed before implementation and then passed; `src/tests/test_admin_settings_ui.py` passed 3/3 and `src/tests/test_health.py` passed 3/3. Touched-file editor diagnostics and `git diff --check` passed. The shared browser rendered `.mojo · fire emoji extension`, and the restarted local API remained healthy at `127.0.0.1:18001` while reporting `1.30.122`.
- **Deployment Note:** Only `cga-desktop-api` was refreshed; PostgreSQL, Redis, FalkorDB, and CGA-Relay were not restarted. The API restart returned the shared browser to the login page, so no password was requested or populated.
- **Packaging:** Built `CGA-Docker-Desktop-1.30.122.zip` with 215 entries and a prebuilt API image; required launcher, release note, and image tar entries were present. The 131,396,450-byte zip has SHA-256 `026e983b929a8f6fa233cd13145e11be9c1caba532e31cb3c7a7c9c2a70e3dda`, with a matching `.sha256` sidecar. Direct packaged-image checks confirmed `APP_VERSION = "1.30.122"` and the `fire emoji extension` label. An initial Python one-liner check failed only because PowerShell truncated a nested quoted string; the direct checks replaced it and passed. No Git tag or remote release was created because Relay remains `1.30.120`, and the release workflow requires the app tag and Relay Cargo version to match.
- **Aggregation:** The sole running Relay process (PID 57452, local control API `127.0.0.1:17860`) accepted official incremental indexing for project `J4BE9NUG2A`. Job `d0cbdd66-b2b4-4351-b5c1-b748cfd13531` completed with status `done` and zero errors after queuing 38 changed files at queue position 0.
- **Next Steps:** No implementation work remains.

## 2026-07-28 Parser Language Icon Badges

- **Current Task:** Add a language-specific icon before every language name in Settings > Indexing while keeping the Mojo extension itself text-only.
- **Root Cause:** Parser language cards contained only the name, recognized files, and enable switch; the template had no visual language identity element or icon metadata.
- **Fix:** Added a complete 51-entry icon map with unique short marks and language-specific colors, a fixed 30-by-30 icon badge inside a stable identity layout, and safe `textContent`/CSS-variable rendering. No CDN, icon package, external image, `innerHTML`, indexing behavior, search behavior, or saved configuration contract changed. Updated README and backend metadata to `1.30.123`.
- **Validation:** The all-language regression test failed before implementation and then passed. The focused UI plus health/version suite passed 7/7, touched-file diagnostics reported no errors, `git diff --check` passed, the live HTML exposed exactly 51 icon entries, and the local API remained healthy at `127.0.0.1:18001` while reporting `1.30.123`. The existing Starlette/httpx deprecation warning remains unrelated.
- **Browser Validation:** Because the shared login session had expired, no password was requested or populated. The loaded production page used the real renderer with temporary in-memory language data that could not be saved. At the integrated browser's 1024-by-720 desktop viewport, all 51 cards had 51 unique nonblank marks, exact 30-by-30 badges, zero card overlaps, zero control collisions, and no group/card overflow. At a 390-by-844 mobile emulation, the grid used one column with zero size errors, collisions, or overflow. Mojo rendered icon mark `Mj` and extension text `.mojo · fire emoji extension`.
- **Deployment Note:** The tested HTML was hot-updated first, then only `cga-desktop-api` was restarted for the `1.30.123` version metadata. PostgreSQL, Redis, FalkorDB, and CGA-Relay were not restarted.
- **Packaging:** Built `CGA-Docker-Desktop-1.30.123.zip` with 215 entries and a validated prebuilt API image. The 131,395,854-byte zip has SHA-256 `246a78b9e39d5a94eabe89895de752f517a02fb2bfc9f9c0178f44aec99e8ce0`, with a matching `.sha256` sidecar. No Git tag or remote release was created because Relay remains `1.30.120`, while the release workflow requires app-tag and Relay Cargo versions to match.
- **Aggregation:** The sole running Relay process (PID 57452, local control API `127.0.0.1:17860`) completed official incremental indexing job `2210d265-723d-460b-af70-4ec5950f9085` for project `J4BE9NUG2A` with status `done` and zero errors after queuing 38 changed files at queue position 0.
- **Next Steps:** No implementation work remains.

## 2026-07-28 Persistent Settings Section Collapse

- **Current Task:** Give all nine top-level Admin Settings sections independent collapse controls and remember the last state per signed-in user.
- **Fix:** Added dynamic section enhancement, stable 28-by-28 collapse buttons, content wrappers, collapsed styling, keyboard-accessible buttons, and `aria-expanded`, `aria-controls`, `aria-label`, and title metadata. Collapse state is stored under `cga_settings_collapsed_sections:<encoded username>` only after `/auth/me` resolves; missing or malformed storage defaults to expanded. Updated README and backend metadata to `1.30.124`.
- **Validation:** The focused regression failed before implementation and passed afterward. `src/tests/test_admin_settings_ui.py` passed 5/5, the Settings plus health suite passed 8/8, the desktop bundle contracts passed 6/6, Python compilation and inline JavaScript parsing passed, touched-file editor diagnostics reported no errors, and `git diff --check` passed.
- **Browser Validation:** The deployed page exposed nine buttons and nine content containers. General and Graph Database collapsed independently while Indexing remained expanded; refresh restored both states and `aria-expanded="false"`. Desktop and 390-by-844 mobile geometry showed no document overflow or heading/button overlap, and the fixed button stayed 28-by-28. Validation-only local-storage keys were removed afterward. The expired Admin session was not bypassed and no stored credential was read.
- **Deployment:** Rebuilt only `cga-desktop-api`; PostgreSQL, Redis, FalkorDB, backup services, volumes, and CGA-Relay were preserved. `http://127.0.0.1:18001/health` reports `status: ok` and version `1.30.124`.
- **Aggregation:** The sole Relay process remained PID 57452. Official project `J4BE9NUG2A` incremental indexing job `9e270111-898e-47e6-8bbf-3b53adb39395` completed with status `done` after accepting 38 changed files through the authenticated CRYSTALS route.
- **Packaging:** Rebuilt `CGA-Docker-Desktop-1.30.124.zip` from the final post-merge source. The final 131,397,471-byte archive has 215 entries, contains app and Relay version `1.30.124`, the collapse feature, and a validated prebuilt API image, excludes secret/generated paths, and has SHA-256 `cc452d5cffeaa5bb38d55a2c85ddf0d0b4f8affe8009fcc2779bf2bba776459d` with a matching sidecar. Generated Compose validation passed.
- **Next Steps:** No implementation work remains.

## 2026-07-28 Parser Language SVG Identity

- **Current Task:** Make every Settings > Indexing parser language visually identifiable and disambiguate Mojo's fire-emoji file extension from its language icon.
- **Fix:** Replaced the 51 plain text badges with deterministic dependency-free inline SVG badges backed by the existing complete icon map. Each language keeps a unique short mark and associated color, while Mojo uses an independent `Mj` badge and displays `.mojo · .🔥 (emoji extension)`. Parser discovery, filtering, persistence, and the canonical backend language catalog are unchanged.
- **Validation:** The focused Admin UI suite passed 5/5 and the combined Admin UI/runtime-config suite passed 14/14. Touched-file diagnostics and `git diff --check` passed. The rebuilt `cga-desktop-api` container is healthy at `127.0.0.1:18001`.
- **Browser Validation:** A read-only preview driven by the canonical backend catalog rendered 51 cards, 51 SVG elements, 51 unique language ids, and the expected 7/34/10 parser groups. At 1440-by-1000 the grid used five columns; at 390-by-844 it used one column. Both had zero horizontal overflow, card overflow, or switch overlap. Mojo rendered `Mj` separately from `.🔥 (emoji extension)`. The shared page id had been repurposed to an unrelated LinkedIn tab, so validation used isolated local pages without credentials or saved settings. The temporary read-only catalog endpoint was stopped afterward.
- **Deployment Note:** Only Compose service `cga` (`cga-desktop-api`) was rebuilt with `--no-deps`; PostgreSQL, FalkorDB, Redis, backup services, volumes, and CGA-Relay were not restarted.
- **Next Steps:** Run post-edit incremental ContextGraph indexing, then review and commit the scoped frontend and regression-test changes when ready.

## 2026-07-28 CGA 1.30.124 Release Preparation

- **Scope:** Consolidated the completed 51-language parser/runtime controls, Settings deep links and per-user collapse state, SVG language identity, Relay global single-instance enforcement and backend-outage alerting, transactional running-process replacement, and Azure Policy authentication compatibility fixes into release `1.30.124`.
- **Validation:** The complete repository Python suite passed 427 tests in `.venv`; the focused changed-surface suite also passed. `cargo test --locked` passed 9 unit and 48 integration tests under default parallel execution after replacing the cross-process mutex startup race with a deterministic two-contender assertion; `cargo fmt --check`, the standalone release replacement/transient-lock harness, editor diagnostics, `git diff --check`, and generated Desktop Compose validation all passed.
- **Relay Release:** `build-secure-release.ps1 -Version 1.30.124` verified CFG, CET, ASLR/NX, writable-executable-section absence, stripped symbol/debug paths, the fixed global mutex, and absence of test scope in the release binary before stopping the old tray. The script replaced one executable and restored exactly one tray as PID 136028 with the original config arguments. Installed, candidate, and dist executable SHA-256 is `13c599c10650d9de4b29269c0b31a6fc6177280723b708b716f7b62a929267e0`; the local unsigned Relay ZIP SHA-256 is `81bed6e9f6fcc568c0252fd530657efd023ed7387090d376256cfef539e87379`; replacement residue count is zero.
- **Runtime:** The restarted loopback controller is enabled for project `J4BE9NUG2A`, reports seven registered projects and a configured session, and exposes the existing incremental-index endpoint. CGA health remains `ok` at version `1.30.124`.
- **Publication Constraint:** Do not upload the unsigned local Relay candidate as a formal asset. Tag `v1.30.124` must run the repository workflow with `-RequireSignature`; publish only after that workflow succeeds.
