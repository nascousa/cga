# Session Scratchpad

# Reserved for temporary AI handoff notes. Keep untracked and non-canonical.
- 2026-05-27 WSR save button: converted Report-tab weekly WSR previews into editable text areas with Save controls for this week and last week. Save persists edits into `cga_wsr_last` plus matching WSR history snapshots, Copy reads current editor text, and Ctrl+S saves the focused report. Bumped CGA/viewer metadata to `1.30.25`, rebuilt viewer assets, synced updated files into `cga-desktop-api`, and restarted the live desktop API. Validation: diagnostics clean, Admin inline JS syntax passed, `src/tests/test_viewer_api.py` passed with default Python (`13 passed`, one existing `asyncio_mode` warning), `git diff --check` passed, `/health` reports `1.30.25`, served Admin HTML contains the Save/editor controls, and browser smoke verified edit -> Save -> localStorage persistence with test state restored afterward. The repo `.venv` pytest path remains blocked by missing `asyncpg`, so default Python was used.
- 2026-05-27 Settings indexing repos folder: added persisted admin runtime config for `indexing.repos_root` (`data/runtime-config.json` or `CGA_RUNTIME_CONFIG_PATH`), exposed it through admin-only `/api/admin/runtime-config` GET/PATCH, wired project repo fallback discovery to prefer the saved repos folder, and added a Settings -> Indexing `Repos Folder` control. Bumped CGA/viewer metadata to `1.30.24`, updated README and architecture/data-flow diagrams, synced backend/frontend/viewer source into `cga-desktop-api`, and saved live `/repos` as the current repos folder. Validation: diagnostics clean; focused pytest passed with system Python (`8 passed`); `.venv` pytest remains blocked by missing `asyncpg`; `.venv` Ruff passed; Admin inline JS syntax passed; scoped `git diff --check` passed; live `/health` reports `1.30.24`; browser verification at `/admin/settings` shows `Repos Folder` with `/repos` and saved status, and authenticated runtime-config API returns `reposRoot=/repos`, `source=saved`, `exists=true`. ContextGraph MCP retrieval sequence could not be run because no `contextgraph_*` MCP tools were available in this VS Code toolset, so bounded local code search was used instead.
- 2026-05-27 Schedule -> BrowserAgent wf5 demo: created CGA Schedule task `OX11IW2V` (`HTTP REQUEST`, project `browseragent`, target `http://host.docker.internal:9885/workflow/run`) to trigger BA `wf5` through a host runner. Initial run proved CGA -> host runner -> BA bridge integration but surfaced a BA wf5 report-index bug on failed history rows; after the BA fixes, repeated Schedule runs succeeded with HTTP 200. Latest success took `138516 ms`; BrowserAgent `results/wf5/latest.json` reports `workflowRunId=20260527-080301-410`, `status=SUCCEEDED`, `healthScore=79`, `totalRetries=10`, `groupsCount=4`, and artifacts at `D:\Repos\BrowserAgent\results\wf5\20260527-run6\run.json`.
- 2026-05-27 Schedule status color follow-up: fixed Admin Schedule `Last Status` summary so it sets a `data-schedule-status` tone and renders failed red, success green, running blue, and empty muted. Bumped CGA/viewer metadata to `1.30.22`, synced updated frontend/backend/viewer files into `cga-desktop-api`, and restarted the live desktop API. Validation: diagnostics clean, Admin inline JS syntax passed, scoped Ruff and `git diff --check` passed, `/health` reports `1.30.22`, and browser computed-style probe confirmed `failed` renders `rgb(239, 90, 90)`; local pytest was blocked by missing `asyncpg` in `.venv`, and container pytest was unavailable because `pytest` is not installed there.
- 2026-05-27 project table follow-up: swapped Admin Project table columns so Project Name appears before Project ID, synced `src/frontend/index.html` into `cga-desktop-api`, and investigated `R5Q1OHYLQ7` (`orcasql-agctools-echo-ops`). Live Redis DB 1 has recent `/repos/orcasql-agctools-echo-ops` index jobs with status `done` (latest `68829492-b357-4f68-bbed-fee0c48fbef6`, 25 files / 37 symbols), XPENDING is 0, and the visible `IDLE` text is the first always-rendered tracker milestone while `SUCCEED` is active. Validation: Admin inline script syntax passed, diagnostics clean, `git diff --check` passed, and browser table headers read Project Name then Project ID.
- 2026-05-27 Schedule BrowserAgent page tests: extended Admin Schedule with BrowserAgent page-test fields (runner URL, page URL, goal, text assertions, artifacts) backed by a high-level `browser_task` payload. CGA now converts those payloads into BrowserAgent command workflows, carries the opened tab ID through subsequent commands, retries text assertions while the page settles, waits for `/result/{id}` command outcomes, compacts screenshot/data URL artifacts before storing run responses, and marks schedule runs failed on BrowserAgent command failures or result timeouts. Live demo task `QN36CRES` ran through Dockerized CGA -> host BA proxy -> BA bridge -> Chrome extension and succeeded against `http://localhost:18001/health` after assertion retry. Updated README, architecture/data-flow diagrams, and CGA/viewer metadata to `1.30.21`. Validation: focused pytest passed, Admin inline script syntax passed, scoped Ruff passed, diagnostics clean, and `git diff --check` passed.
- 2026-05-27 Schedule task ID follow-up: shortened unique external `task_id` values to 8 uppercase alphanumeric characters, startup migration/backfill now rewrites missing or non-8-character `scheduled_tasks.task_id` values, and copy icons remain beside Schedule task IDs in the Admin UI. Bumped CGA/viewer metadata to `1.30.20`. Validation: focused pytest `6 passed`, scoped Ruff passed, inline Admin JS syntax passed, live desktop container synced/restarted, `/health` reports `1.30.20`, and browser verification shows the Schedule table with 8-character Task ID `WPBE2W0H` plus `Copy Task ID` button.
- 2026-05-27 CPMD follow-up: added a Settings -> Delegation Microsoft device-code prompt warning that MSFTVPN MANUL must be enabled before sign-in, synced the updated Admin HTML into `cga-desktop-api`, and prepared the full ADO delegation/schedule batch on `dev/cga-ado-delegation-schedules`. Validation: targeted pytest `23 passed` with pytest plugins enabled, scoped Ruff passed, Admin inline JS syntax passed, viewer `npm run build` passed, `git diff --check` passed, and served Admin HTML contains the MSFTVPN MANUL prompt.
- 2026-05-27 Schedule admin page: added persisted `scheduled_tasks` / `scheduled_task_runs`, admin schedule CRUD + Run Now API, a background due-task worker, and an Admin `Schedule` tab beside Project for configuring BrowserAgent tasks, agent activation HTTP calls, and generic HTTP jobs. Bumped CGA/viewer metadata to `1.30.18`. Validation: focused pytest `6 passed`, focused Ruff passed, inline Admin JS syntax passed, live desktop container synced/restarted, `/health` reports `1.30.18`, and browser verification shows Schedule beside Project with an empty schedule grid.
- 2026-05-27 delegation copy controls: added copy-icon buttons beside the Microsoft device sign-in link and device code in Settings -> Delegation, backed by the existing `copyText` helper and a reusable `renderCopyButton`. Bumped CGA/viewer metadata to `1.30.18`. Validation: diagnostics clean, focused pytest `20 passed`, viewer build passed, desktop stack rebuilt, `/health` reports `1.30.18`, and served HTML includes `Sign-in link` and `Device code` copy controls.
- 2026-05-27 delegation UI move: moved the Microsoft/Azure DevOps ticket lookup connected-account card out of `/admin/report` and into Settings -> Delegation. Added the Delegation subtab, status refresh on subtab activation, and bumped CGA/viewer metadata to `1.30.17`. Validation: focused pytest and Ruff passed; desktop stack rebuilt; served HTML confirms Report no longer contains `microsoft-ado-connection-card` and Settings Delegation does.
- 2026-05-26 Microsoft/Azure DevOps report enrichment: added encrypted `oauth_connections` storage, `external_ticket_cache`, Microsoft device-code connect/status/disconnect endpoints, Azure DevOps work item/PR enrichment for `/api/admin/work-briefing?include_external=true`, and a Report-tab connected-account card. Bumped CGA/viewer versions to `1.30.16`, updated README metadata and architecture/data-flow diagrams. Validation: focused pytest (`test_oauth_connection_storage.py`, `test_azure_devops_enrichment.py`, `test_work_briefing_api.py`, `test_viewer_api.py`) passed with 20 tests; focused Ruff passed; viewer `npm run build` passed; desktop stack rebuilt and `/health` reports `1.30.16` with `oauth_connections` and `external_ticket_cache` present.
- 2026-05-26 login background performance follow-up: the initial `mouseControls`/`touchControls` disable was insufficient in the VS Code embedded browser, so the login background was fully downgraded from `three.js` + `VANTA.NET` to static CSS in `src/frontend/index.html`. Removed the external WebGL scripts, deleted the Vanta init/destroy logic, kept the existing login visuals via layered gradients, and synced the updated HTML into both live API containers (`cga-desktop-api`, `contextgraph-api-dev`). Focused validation: `get_errors` on `src/frontend/index.html` and `README.md` returned no errors; live `http://localhost:18001/admin/projects` HTML no longer contains `vanta.net`, `three.min.js`, `VANTA.NET`, or `_loginVanta`.
- 2026-05-15 CGA rename: renamed public-facing brand to `CGA (ContextGraphAdmin)`, bumped release/runtime/viewer version to `1.29.83`, changed release assets to `cga-*`, GHCR image to `cga-api`, UI titles to CGA, health service to `cga`, MCP server name to `cga`, and dev/release container names to `cga-*`. Compose config and focused health/viewer/admin tests passed.
- 2026-05-15 license readiness: added MIT `LICENSE`, documented it in README and publishing preflight, and bumped release/runtime/viewer version references to `1.29.84`.
- 2026-05-15 nascousa migration: per user direction, ContextGraph/CGA source must no longer be checked in to `nasco_microsoft`; public source distribution now targets `https://github.com/nascousa/cga`, release image references target `ghcr.io/nascousa/cga-api`, and release/runtime/viewer version references were bumped to `1.29.85`.
- 2026-05-11 AST diagram: added `.adc/knowledge/diagrams/ast-workflow.mmd` to explain source text -> tokens -> parser -> AST -> symbol/semantic checks -> ContextGraph edges -> safe transform/repair loop. Rendered the Mermaid diagram successfully.
- 2026-05-11 viewer performance button layout follow-up: fixed the clipped `Performance On` control by allowing `.field-row` controls to wrap and giving side-panel buttons a stable `flex: 1 1 120px` basis. Bumped viewer/Admin cache keys and README metadata to `1.29.81`, added static CSS coverage, copied updated assets into local `contextgraph-api-dev`, and browser-verified `/admin/graph` serves `1.29.81`, the Performance button is not clipped, and loading `osagent` still renders 250 nodes and 212 edges across seven Sigma canvases.
- 2026-05-11 viewer 2.5D performance mode: kept the Sigma/Graphology 2.5D projection path and added worker-backed deterministic position preprocessing, typed-array projection storage, zoom-aware node LOD, edge throttling, aggregate cluster nodes, hover-neighborhood edge focus, lighter scheduled refreshes, and a `Performance On` toggle. No full 3D/Three.js dependency was introduced.
- Updated viewer/admin cache keys and README metadata to `1.29.80`, added `/viewer/worker.js`, and documented the scale behavior in `src/viewer/README.md`.
- Validation: `npm run build` in `src/viewer` passed; focused viewer tests passed with `11 passed, 1 warning` using `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .\.venv\Scripts\python.exe -m pytest -o addopts= src/tests/test_viewer_api.py -q` (warning: existing unknown `asyncio_mode` config option).
- Live check: copied updated static files into local `contextgraph-api-dev`; `/admin/graph` now serves `/viewer/?v=1.29.80`, `main.js?v=1.29.80`, `styles.css?v=1.29.80`, and no-cache `/viewer/worker.js`. Browser smoke loaded the `osagent` graph with 250 nodes and 212 edges, seven visible Sigma canvases, `Performance On`, and the cluster overlay element present. Performance toggle off/on and 3D Rotate start/stop both worked.
- Browser automation note: one strict wait timed out while the Load button was still busy, but the graph completed loading and subsequent DOM inspection verified the expected counts and controls.
- 2026-05-11 presentation revision: reduced deck to 12 slides and integrated AST as a core pillar alongside ContextGraph, covering hallucination mitigation, engineering quality, token optimization, and time efficiency in English.
- 2026-05-11 presentation update: Refactored `docs/contextgraph-value-presentation-slides.md` into stricter slide-based format (numbered slide flow, one-message-per-slide layout, shorter bullets for presenter readability).
- 2026-05-11 diagram refresh: rewrote all `.adc/knowledge/diagrams/*.mmd` files to match the current ContextGraph runtime verified from `src/backend/main.py`, `backend/tools/server.py`, `backend/graph/registry.py`, `backend/indexer/consumer.py`, `backend/queue/streams.py`, `backend/viewer/router.py`, and `backend/agent/query_strategy.py`.
- Replaced stale generic RA/CA/EA/VA diagrams with current FastAPI admin and viewer surfaces, MCP SSE flow, project-token middleware, per-project FalkorDB graphs, Redis queue and cache usage, trace evaluation, and CG-first local fallback behavior.
- Validation: all five Mermaid diagrams passed `mermaid-diagram-validator`, and all five previews opened successfully after one label fix in `data-flow.mmd`.
- 2026-04-19: Added CG MCP server-side git-aware indexing design.
- New MCP tool: `index_repo_changes(repo_path, include_untracked=true, auto_full_on_destructive=true)`.
- Rationale: periodic/manual reindex is a cross-project operational gap; moved change discovery into CG MCP server instead of requiring each downstream repo to script it.
- Safety note: deletes/renames auto-promote to full index because current incremental pipeline does not clean stale symbols for disappeared files.
- 2026-04-19 update: native pipeline cleanup added for repo/file subgraphs.
- `IndexPipeline.index_full()` now clears the repo subgraph before rebuild.
- `IndexPipeline.index_incremental()` now removes stale file-local graph data for missing, deleted, renamed, and rewritten files before re-writing current symbols/variables/edges.
- `index_repo_changes()` now defaults to incremental even for destructive git changes; `auto_full_on_destructive=true` is optional fallback only.
- 2026-04-20 update: Admin queue telemetry added to `/auth/projects/index-status`.
- `IndexJobStatus` now includes `queue_position` and `eta_seconds` for best-effort queue wait estimation.
- Queue snapshot is sourced from Redis status keys via `JobConsumer.get_queue_snapshot()`.
- Fixed repo path variant issue in status aggregation: now merges jobs across all candidate repo paths (`D:/Repos/...` + `/repos/...`) and selects the newest `updated_at` record.
- Frontend project status text now appends queue/ETA when available (e.g., `pending · q#2 · ETA ~75s`).
- 2026-04-20 update: Reworked project indexing UI into a shipping-style tracker.
- Admin project rows now render all lifecycle nodes (`Queued`, `Indexing`, `Complete`, `Failed`) at once and pulse the current active node until a terminal state is reached.
- Status detail chips now always show `Status`, `Queue`, `ETA`, `Scope`, and `Source`, even when queue telemetry is unavailable (`q#-`, `ETA -`).
- 2026-04-20 update: expanded project panel now includes `Recent Index Events` history built from the latest 5 jobs returned by `/auth/projects/index-status`.
- Added backend `recent_jobs` payload on `ProjectIndexStatus` and kept `latest_job` as the headline status for the main row.
- 2026-04-20 final tracking UX pass:
- Strengthened tracker connectors with explicit completed/current progress lines.
- Added relative time labels (`just now`, `15s ago`, `1m ago`, etc.) for queued/updated timestamps in recent job history.
- Failed history events now expose `Retry` and expandable `Show failure details` UI, reusing the existing project reindex endpoint.
- 2026-04-20 tracker alignment tweak: changed the main tracker grid to `inline-grid` with content-sized columns and left-aligned parent panel so the entire shipping tracker sits flush left instead of stretching across the full cell width.
- Validation blocker: required Docker rebuild via remote daemon `tcp://192.168.1.239:2375` timed out while resolving `falkordb/falkordb:latest`, so live browser verification of the alignment change is pending until the remote daemon is reachable again.
- 2026-04-20 local verification update: local `docker compose --profile dev up -d --build api-dev` succeeded, and the served admin HTML now contains the left-alignment CSS for both the main tracker (`inline-grid`, parent `align-items: flex-start`) and the expanded `Recent Index Events` timeline (`width: min(100%, 760px)`, `justify-content: flex-start`, `align-self: flex-start`).
- 2026-04-20 wording cleanup: changed the queue placeholder from `q#-` to `—` in both the main tracker summary and `Recent Index Events` so the UI no longer exposes an internal-looking placeholder when no queue position is available. Also changed `ETA -` to `—` for visual consistency.
- 2026-04-20 conditional field visibility: Queue and ETA fields are now hidden when job status is not pending/processing, keeping the interface cleaner by only showing queue/ETA info when relevant (during active indexing).
# Agent Session State / Brain Dump

**Objective:**
Write down exactly what you are currently doing, the last known successful step, and any immediate blockers.
This ensures the NEXT agent handling this repository knows exactly where you left off.

- **Current Task:** 2026-05-07 ContextGraph HPS context-quality benchmark.
- **Last Action Taken:** Added deterministic Hallucination Pressure Score scoring, REST/MCP benchmark surfaces, a CodexCLI/ClaudeCLI JSONL manifest, a report runner, generated JSON/Markdown reports, and README/docs updates.
- **Failing Tests / Errors:** Normal pytest collection still hits local `pytest_asyncio` AttributeError before test execution; new runner initially needed an explicit `src/` import path fix when launched with `python -m src.scripts...`.
- **Next Steps:** Focused HPS tests passed with plugin autoload disabled and pytest addopts cleared; run full suite after the local pytest plugin mismatch is resolved.
- 2026-05-08 update: Added a standalone large graph viewer under `src/viewer` and a protected `/api/viewer/graphs/{project_name}` API for stats plus chunked graph loading.
- 2026-05-08 update: Merged the graph viewer into the Admin SPA as an admin-only `Graph Viewer` tab backed by the same `/viewer/` static app and shared `cg_jwt` session.
- 2026-05-08 update: Added relationship-type colors to the viewer: CALLS, IMPORTS, DEFINES, CONTAINS, USES_VARIABLE, and FLOWS_TO now use distinct node/link colors with a legend.
- Library decision: selected MIT-licensed `@cosmos.gl/graph` for GPU/WebGL large network rendering; the API defaults to 50k-edge chunks and caps chunks at 100k edges so million-scale graphs are loaded progressively instead of as one JSON payload.
- Integration note: FastAPI now serves the viewer at `/viewer`; it reuses the Admin UI `cg_jwt` token or accepts a pasted admin JWT.
- 2026-05-09 update: Replaced the viewer renderer with `sigma` + `graphology`, using deterministic 3D coordinates projected into Sigma with perspective/depth styling. The viewer now renders every loaded node/edge up to a 500,000-node client cap and raises the API chunk ceiling to 500,000 edges.
- 2026-05-09 cache follow-up: Viewer static assets now use no-store headers; the Admin iframe points to `/viewer/?v=1.29.60`; and the viewer references `styles.css?v=1.29.60` plus `main.js?v=1.29.60` so browsers with old cached assets fetch the Sigma viewer immediately after rebuild.
- 2026-05-09 load follow-up: Reworked viewer chunks to use relationship-id cursor pagination instead of `SKIP`, and set a 60s viewer query timeout for large windows after a live 50k-edge load initially timed out.
- Validation: `npm run build` in `src/viewer` passed, and `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .\.venv\Scripts\python.exe -m pytest -o addopts= src/tests/test_viewer_api.py -q` passed with 8 tests. Rebuilt `api-dev`, loaded `/viewer/?v=1.29.60`, rendered 28,087 nodes plus 50,000 edges from the `osagent` graph, and verified the 3D Rotate control starts without page errors. Initial dependency install was retried with an absolute ContextGraph path after a persisted terminal cwd mismatch; no HermesAgent git changes were present.
- 2026-05-09 auth follow-up: Removed the visible Session Token field and Save/Forget buttons from the viewer. The viewer still reads the Admin UI `cg_jwt` value from same-origin browser storage for API calls, so users should sign in through `/admin` instead of manually pasting a token.
- Validation: `npm run build` in `src/viewer` passed, and `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .\.venv\Scripts\python.exe -m pytest -o addopts= src/tests/test_viewer_api.py -q` passed with 8 tests after updating the token UI assertions. Rebuilt `api-dev`, loaded `/viewer/?v=1.29.62`, and verified the DOM has no `Session Token`, `#token-input`, `#save-token`, or `#forget-token` while loading `main.js?v=1.29.62`.
- 2026-05-09 label/defaults follow-up: Viewer labels are now hover-only by disabling Sigma's default label pass and removing forced labels; the default chunk input is 1,000; the 3D Rotate window is 60 seconds; and status text no longer appends the `Sigma 3D projection` summary. The live Docker Desktop `contextgraph-api-dev` container was rebuilt after the remote Docker daemon at `tcp://192.168.1.239:2375` timed out.
- Validation: `npm run build` in `src/viewer` passed, and `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .\.venv\Scripts\python.exe -m pytest -o addopts= src/tests/test_viewer_api.py -q` passed with 8 tests. Rebuilt `contextgraph-api-dev`, loaded `/viewer/?v=1.29.63`, verified chunk size `1000`, loaded 844 nodes plus 1,000 edges, verified the old `Sigma 3D projection` text is not visible, confirmed the default labels canvas is blank, and confirmed node hover activates the Sigma hover layer.
- 2026-05-09 hover contrast follow-up: Replaced Sigma's default hover bubble with a custom high-contrast dark tooltip, brighter border, larger bold label text, and a clearer hover ring. Bumped viewer cache assets and Admin iframe to `/viewer/?v=1.29.64`.
- Validation: `npm run build` in `src/viewer` passed, and `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .\.venv\Scripts\python.exe -m pytest -o addopts= src/tests/test_viewer_api.py -q` passed with 8 tests. Rebuilt `contextgraph-api-dev`, loaded `/viewer/?v=1.29.64`, loaded 844 nodes plus 1,000 edges, and verified the hover canvas contains the new dark tooltip fill, bright text, and cyan border pixels.
- 2026-05-09 control defaults follow-up: Renamed the chunk control label to `Display Nodes`, set its default value to 250, started `Uses variable` and `Flows to` unchecked, made edges hidden by default with a fresh localStorage preference key, renamed `Next Chunk` to `Load more`, and bumped viewer/Admin cache keys to `/viewer/?v=1.29.65`.
- Validation: `npm run build` in `src/viewer` passed, and `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .\.venv\Scripts\python.exe -m pytest -o addopts= src/tests/test_viewer_api.py -q` passed with 8 tests. Rebuilt `contextgraph-api-dev`, loaded `/viewer/?v=1.29.65`, confirmed `Display Nodes` value `250`, unchecked `Uses variable` and `Flows to`, hidden-edge default via `Show Edges`/`aria-pressed=false`, and loaded a 250-edge batch with the `Load more` control enabled.
- 2026-05-09 node visibility/FPS follow-up: Added View-section node kind checkboxes for Repository, File, Symbol, Variable, and other Node records, wired live node hiding to also suppress connected edges, added a bottom-right FPS badge, reset the edge visibility preference key so edges start hidden again, and bumped viewer/Admin cache keys to `/viewer/?v=1.29.67`.
- Validation: `npm run build` in `src/viewer` passed, and `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .\.venv\Scripts\python.exe -m pytest -o addopts= src/tests/test_viewer_api.py -q` passed with 8 tests. Rebuilt `contextgraph-api-dev`, loaded `/viewer/?v=1.29.67`, confirmed `main.js`/`styles.css` cache keys, `Display Nodes` value `250`, `Uses variable` plus `Flows to` unchecked, `Show Edges`/`aria-pressed=false`, all five node type controls checked, FPS updating, loaded a 250-edge batch, toggled File nodes off/on, and left all node types visible.
- 2026-05-09 load placement follow-up: Moved the primary `Load` button from the Project row to the Window action row beside `Clear`, preserving `Load more`, `Display Nodes=250`, unchecked `Uses variable`/`Flows to`, and hidden-edge default. Bumped viewer/Admin cache keys to `/viewer/?v=1.29.68`.
- Validation: `npm run build` in `src/viewer` passed, and `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .\.venv\Scripts\python.exe -m pytest -o addopts= src/tests/test_viewer_api.py -q` passed with 8 tests. Rebuilt `contextgraph-api-dev`, loaded `/viewer/?v=1.29.68`, confirmed `main.js`/`styles.css` cache keys, `Display Nodes=250`, `Uses variable` and `Flows to` unchecked, hidden-edge default via `Show Edges`/`aria-pressed=false`, action row order `Load more`, `Load`, `Clear`, and clicked the moved `Load` button to load 270 nodes plus 250 edges.
- 2026-05-09 node-count loading follow-up: Changed `/api/viewer/graphs/{project}/chunk` so `limit` pages by unique graph nodes, fetches edges only between that selected node set, and returns at most the requested Display Nodes count. Restored the `Display Nodes` label, kept `Load` immediately left of `Load more`, reset the edge visibility preference key so edges default hidden in existing browsers, and bumped viewer/Admin cache keys to `/viewer/?v=1.29.72`.
- Validation: `npm run build` in `src/viewer` passed, and `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .\.venv\Scripts\python.exe -m pytest -o addopts= src/tests/test_viewer_api.py -q` passed with 9 tests. Rebuilt `contextgraph-api-dev`, loaded `/viewer/?v=1.29.72`, confirmed `Display Nodes=250`, `Uses variable` and `Flows to` unchecked, hidden-edge default via `Show Edges`/`aria-pressed=false`, action row order `Load`, `Load more`, `Clear`, clicked `Load`, and verified the graph loaded exactly 250 nodes with 56 intra-page edges.
- 2026-05-09 admin deep-link follow-up: Added path-based Admin tab routes (`/admin/projects`, `/admin/users`, `/admin/audit`, `/admin/graph`) plus a FastAPI SPA fallback for direct loads and browser refreshes. The Admin topbar now updates history on tab changes and handles back/forward navigation.
- Validation: `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -o addopts= src/tests/test_viewer_api.py::test_admin_embeds_versioned_graph_viewer src/tests/test_viewer_api.py::test_admin_deep_links_are_served -q` passed. The full `src/tests/test_viewer_api.py` run still fails one unrelated stale viewer assertion expecting `ROTATION_RUN_MS = 60000` in `/viewer/main.js`. Deployed `src/frontend/index.html` and `src/backend/main.py` into `contextgraph-api-dev`, confirmed container health, verified all four Admin deep links return HTTP 200, and browser-tested direct `/admin/users`, tab navigation to `/admin/graph` and `/admin/audit`, plus back navigation to Graph.
- 2026-05-09 edge legend source follow-up: Replaced hard-coded edge filter colors in `src/viewer/index.html` with generated controls in `src/viewer/main.js`. The legend dot `--edge-color` now comes from `EDGE_STYLES`, the same source used by rendered Sigma edge attributes (`color: style.color`). Bumped README and viewer/Admin cache keys to `1.29.75`.
- Validation: `npm run build` in `src/viewer` passed, and `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -o addopts= src/tests/test_viewer_api.py -q` passed with 10 tests. Deployed `src/viewer/index.html` and `src/viewer/main.js` into `contextgraph-api-dev`, confirmed container health, verified deployed source contains both legend and rendered edge `style.color` usage, loaded `/admin/graph`, checked generated dot colors (`#4ae387`, `#5badff`, `#ffd642`, `#adb8cc`, `#42e694`, `#4ae387`), and loaded an `osagent` chunk with 40 nodes plus 56 visible edges.
- 2026-05-09 edge filter cleanup follow-up: Removed edge color dots from the generated edge filter labels while preserving checkbox behavior and rendered graph edge colors. Bumped README and viewer/Admin cache keys to `1.29.76`.
- Validation: `npm run build` in `src/viewer` passed, and `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -o addopts= src/tests/test_viewer_api.py -q` passed with 10 tests. Deployed `src/viewer/index.html`, `src/viewer/main.js`, `src/viewer/styles.css`, and `src/frontend/index.html` into `contextgraph-api-dev`, confirmed container health, verified live `v=1.29.76` source has no `edge-dot` or `--edge-color`, and browser-checked `/admin/graph` iframe DOM with `edgeDotCount=0` plus `edgeColorStyleCount=0`.
- 2026-05-09 visible node loading follow-up: Confirmed the first `osagent` API chunk with `limit=250` returned only 40 displayable points because the backend scans 250 raw node IDs and prunes nodes without selected in-window edges. Updated the viewer so `Display Nodes` means visible/rendered nodes: loading now continues through backend chunks until the requested visible count is reached, the graph/filter is exhausted, or the scan safety limit is hit. Bumped README and viewer/Admin cache keys to `1.29.77`.
- Validation: `npm run build` in `src/viewer` passed, and `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -o addopts= src/tests/test_viewer_api.py -q` passed with 10 tests. Deployed `src/viewer/index.html`, `src/viewer/main.js`, and `src/frontend/index.html` into `contextgraph-api-dev`, confirmed container health, verified live `v=1.29.77` source, and browser-tested `/admin/graph` with `Display Nodes=250`: the viewer loaded exactly 250 visible nodes, 56 edges, and cursor `460` after scanning beyond the sparse first raw node window.
- 2026-05-09 Falkor copy follow-up: Added an icon-only copy control beside the viewer `Browser` button to copy `falkor://contextgraph-falkordb-dev:6379`. Bumped README and viewer/Admin cache keys to `1.29.78`.
- Validation: `npm run build` in `src/viewer` passed, and `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .\.venv\Scripts\python.exe -m pytest -o addopts= src/tests/test_viewer_api.py -q` passed with 10 tests. Copied updated static files into the running local `contextgraph-api-dev` container, confirmed health, reloaded `/admin/graph`, verified the `Copy FalkorDB connection URL` button appears next to `Browser`, and confirmed clicking it replaces a sentinel clipboard value with the Falkor URL. Browser direct clipboard read remained permission-blocked, so verification used paste-based clipboard checking.
- 2026-05-09 Load more removal follow-up: Removed the viewer `Load more` button and all `loadNext` DOM/event wiring, leaving the action row as `Load` and `Clear`. Bumped README and viewer/Admin cache keys to `1.29.79` and updated viewer docs/tests.
- Validation: `npm run build` in `src/viewer` passed, and `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .\.venv\Scripts\python.exe -m pytest -o addopts= src/tests/test_viewer_api.py -q` passed with 10 tests. Copied updated static files into the running local `contextgraph-api-dev` container, confirmed health, reloaded `/admin/graph`, and browser-verified the iframe has no `#load-next`, uses `main.js?v=1.29.79` plus `styles.css?v=1.29.79`, and shows the buttons `Refresh`, `Load`, `Clear`, `Fit`, `3D Rotate`, and `Hide Edges`.
