# Web / MCP layer
fastapi>=0.136.3,<1.0
uvicorn[standard]>=0.48.0,<1.0
mcp[cli]>=1.27.2,<2.0
pydantic>=2.13.4,<3.0

# Auth
# aiosqlite removed — auth now runs on asyncpg via backend/auth/pgshim.py
bcrypt>=5.0.0,<6.0
python-jose[cryptography]>=3.5.0,<4.0

# WorkBriefing / pgvector storage
asyncpg>=0.31.0,<1.0

# Message queue
redis[hiredis]>=8.0.0,<9.0

# Graph database
falkordb>=1.6.1,<2.0

# Code parsing
tree-sitter>=0.25.2,<1.0

# Semantic embeddings
# sentence-transformers>=5.0 required: CVE-2026-1839 affects <5.0
sentence-transformers>=5.5.1,<6.0
numpy>=2.4.6,<3.0

# Observability
structlog>=25.5.0,<26.0
prometheus-client>=0.25.0,<1.0

# Quality gate (dev / CI)
pytest>=9.0.3,<10.0
pytest-cov>=7.1.0,<8.0
pytest-asyncio>=1.4,<2.0
httpx>=0.28.1,<1.0
# black>=25.1 required: CVE-2026-32274 affects 24.x
black>=26.5.1,<27.0
ruff>=0.15.15,<1.0
mypy>=2.1.0,<3.0
pip-audit>=2.10.0,<3.0
